# Auto-generated by render_pod_spec.py — do not edit by hand.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lighter-redis
  labels:
    app: lighter-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lighter-redis
  template:
    metadata:
      labels:
        app: lighter-redis
    spec:
      tolerations:
      - key: "dedicated"
        operator: "Equal"
        value: "zkp-prover"
        effect: "NoSchedule"
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: redis-service
spec:
  selector:
    app: lighter-redis
  ports:
  - protocol: TCP
    port: 6379
    targetPort: 6379
