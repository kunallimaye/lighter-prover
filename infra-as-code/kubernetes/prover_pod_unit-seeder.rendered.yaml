# Auto-generated by render_pod_spec.py --emit-fungible — do not edit by hand.
apiVersion: batch/v1
kind: Job
metadata:
  name: lighter-seeder
  labels:
    app: zkp-seeder
spec:
  template:
    metadata:
      annotations:
        gke-gcsfuse/volumes: "true"
      labels:
        app: zkp-seeder
    spec:
      serviceAccountName: prover-sa
      restartPolicy: Never
      tolerations:
      - key: "dedicated"
        operator: "Equal"
        value: "zkp-prover"
        effect: "NoSchedule"
      containers:
      - name: seeder
        image: us-east4-docker.pkg.dev/kunal-scratch/lighter-prover-iac/zkp-prover:amd64
        command: ["prover-node", "work"]
        args:
        - "--transport=pubsub"
        - "--seed"
        - "--blocks=$(BLOCKS)"
        - "--radix=$(RADIX)"
        - "--tx-per-proof=$(TX_PER_PROOF)"
        - "--fold-strategy=$(FOLD_STRATEGY)"
        - "--ack-deadline=$(ACK_DEADLINE)"
        env:
        - name: PROVER_PUBSUB_PROJECT
          value: "kunal-scratch"
        - name: PROVER_PUBSUB_TOPIC
          value: "stark-proofs-topic"
        - name: PROVER_PUBSUB_SUBSCRIPTION
          value: "tree-aggregators-sub"
        - name: PROVER_PUBSUB_EVENT_TOPIC
          value: "prover-events-topic"
        - name: PROVER_PUBSUB_BUCKET
          value: "kunal-scratch-tfstate"
        - name: PROVER_PUBSUB_OBJECT_PREFIX
          value: "benchmark-reports/attempt-50/amd64/c3d/stark_proofs/"
        - name: BLOCKS
          value: "1"
        - name: ACK_DEADLINE
          value: "180"
        - name: RADIX
          value: "16"
        - name: TX_PER_PROOF
          value: "4"
        - name: FOLD_STRATEGY
          value: "hex"
        - name: RUST_LOG
          value: "info"
        resources:
          requests:
            cpu: "500m"
            memory: "512Mi"
          limits:
            cpu: "1"
            memory: "1Gi"
        volumeMounts:
        - name: gcs-volume
          mountPath: /data/reports
      volumes:
      - name: gcs-volume
        csi:
          driver: gcsfuse.csi.storage.gke.io
          volumeAttributes:
            bucketName: "kunal-scratch-tfstate"
            mountOptions: "only-dir=benchmark-reports/attempt-50/amd64/c3d,implicit-dirs"
            metadataCacheTTLSeconds: "0"
            metadataStatCacheCapacity: "0"
