# Project Agent Rules - Lighter Prover

## Next Release Asset Mandate (`v0.0.2+`)
When drafting, proposing, or creating any subsequent GitHub Release for this repository (e.g., `v0.0.2`, `v0.1.0`), you MUST attach `reports/proposal_phase2_async_pipelining.md` as an official downloadable release asset alongside the benchmark matrix CSV.

## Capstone Benchmark Master Plan Mandate
Whenever planning, executing, resuming, or verifying any comparative Capstone Benchmark Run (`make capstone-benchmark-run`, `cloud-bench-run`, `cloud-run-distributed-cluster`), agents MUST reference and strictly follow the master execution specifications, unified Partition ID formatting (`benchmark-id-ALL-<local_timestamp>`), fail-fast canary preflight protocol, and Resumable Storage Checkpointing architecture documented in `reports/capstone_benchmark_master_plan.md`.
